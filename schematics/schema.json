{
  "$schema": "http://json-schema.org/schema",
  "$id": "FeatureSchema",
  "title": "Feature Generation Options",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The feature name. Used for module, controller, service, DTOs, and decorators."
    },
    "path": {
      "type": "string",
      "description": "The base directory where the feature folder will be created.",
      "default": "src"
    },
    "flat": {
      "type": "boolean",
      "description": "If true, generates files directly in the target path without a dedicated subfolder.",
      "default": false
    }
  },
  "required": ["name"]
}
